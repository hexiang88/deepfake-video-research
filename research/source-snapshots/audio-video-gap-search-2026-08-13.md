# 音视频缺口定向搜索快照

核验日期：2026-08-13。通过用户指定代理访问 GitHub 官方 API。搜索结果仅用于发现候选，不证明作者/机构官方性、代码完整性或指标有效性。

## 请求记录

- 本批次计划：先读取 1 次 `rate_limit`，再执行 2 次窄范围搜索；不重复读取已有快照中的仓库。
- `rate_limit`：HTTP 200；核心资源 `limit=60`、`remaining=51`、`used=9`、`reset=1786590267`。
- 搜索 1：`ASVspoof voice conversion detection paper`，HTTP 200，搜索剩余头为 4，未返回候选列表。
- 搜索 2：`LipFD audio visual deepfake detection`，HTTP 200，搜索剩余头为 3；返回 `Khushi-2106/LipSync-Deepfake-Detection`。
- 因搜索配额接近耗尽且没有明确的作者/机构官方新增候选，本轮停止全部 GitHub 请求；未收到 429。

## 搜索候选处理

### `Khushi-2106/LipSync-Deepfake-Detection`

- 搜索摘要称其扩展 LipFD，并加入显式光流建模。
- 当前只有搜索结果，未读取仓库 API 元数据、README、论文或结果文件。
- 无法确认作者与 LipFD 原作者/机构的关系，也无法核验数据集、偏移量协议、划分、生成器或指标。
- 处理：保持待核验，不计入视频唇音同步官方方案，不作为 LipFD 的官方实现证据。

## 缺口结论

- 本批次没有新增可进入音频或视频主报告的官方一手实现或绑定指标。
- 音频 VC/TTS/克隆与实时检测的逐攻击、跨生成器和延迟证据仍缺失。
- 视频 Talking Face、唇音同步的偏移量分层、生成器隔离、数据划分和退化协议仍缺失。
- 音频和视频各细分方向继续保持 `深核验`，没有方向可改为 `完成`。
